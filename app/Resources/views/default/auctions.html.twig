<div class="list-group">
{% for auction in auctions %}
    <a class="list-group-item" href="#">
        <span class="badge">{{ auction.getBids.count }}</span>
        {{ auction.title }}
    </a> 
{% endfor %}
</div>
